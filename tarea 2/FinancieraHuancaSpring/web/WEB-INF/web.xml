<?xml version="1.0" encoding="UTF-8"?>
<web-app xmlns="https://jakarta.ee/xml/ns/jakartaee"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="https://jakarta.ee/xml/ns/jakartaee https://jakarta.ee/xml/ns/jakartaee/web-app_6_0.xsd"
         version="6.0">

    <display-name>FinancieraHuancaSpring</display-name>

    <!-- Filtro de Spring que fija UTF-8 en cada peticion, para que los
         acentos y la ñ del formulario lleguen bien al servidor. -->
    <filter>
        <filter-name>codificacion</filter-name>
        <filter-class>org.springframework.web.filter.CharacterEncodingFilter</filter-class>
        <init-param>
            <param-name>encoding</param-name>
            <param-value>UTF-8</param-value>
        </init-param>
        <init-param>
            <param-name>forceEncoding</param-name>
            <param-value>true</param-value>
        </init-param>
    </filter>
    <filter-mapping>
        <filter-name>codificacion</filter-name>
        <url-pattern>/*</url-pattern>
    </filter-mapping>

    <!-- El "front controller" de Spring MVC: recibe TODAS las peticiones y
         se las reparte a los metodos del Controller segun la URL. Sus beans
         se declaran en WEB-INF/spring-servlet.xml (el nombre del servlet,
         "spring", es el que da el nombre a ese archivo). -->
    <servlet>
        <servlet-name>spring</servlet-name>
        <servlet-class>org.springframework.web.servlet.DispatcherServlet</servlet-class>
        <init-param>
            <param-name>contextConfigLocation</param-name>
            <param-value>/WEB-INF/spring-servlet.xml</param-value>
        </init-param>
        <load-on-startup>1</load-on-startup>
    </servlet>
    <servlet-mapping>
        <servlet-name>spring</servlet-name>
        <url-pattern>/</url-pattern>
    </servlet-mapping>

</web-app>
