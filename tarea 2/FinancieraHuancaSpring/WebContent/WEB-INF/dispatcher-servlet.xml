<?xml version="1.0" encoding="UTF-8"?>
<!--
    Tarea 2 - Tecnologias para Desarrollos en Internet.

    Beans del DispatcherServlet, con la misma forma que el
    dispatcher-servlet.xml del ejemplo FormularioDBSpring3MVC.
-->
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:p="http://www.springframework.org/schema/p"
	xmlns:context="http://www.springframework.org/schema/context"
	xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context.xsd">

	<!-- Traduce el nombre que devuelve el Controller ("creditoForm") al JSP
	     que lo pinta: /WEB-INF/jsp/creditoForm.jsp -->
	<bean id="viewResolver"
          class="org.springframework.web.servlet.view.InternalResourceViewResolver" p:prefix="/WEB-INF/jsp/" p:suffix=".jsp" />

	<!-- Mensajes de validacion (src/messages.properties) -->
	<bean id="messageSource" class="org.springframework.context.support.ResourceBundleMessageSource" p:basename="messages" />

	<!-- Busca los @Controller -->
	<context:component-scan base-package="control" />

	<bean id="creditoService" class="servicio.CreditoServiceImpl" />

	<bean id="creditoValidator" class="validator.CreditoValidator" />

</beans>
